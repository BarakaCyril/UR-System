Step 1: Discovery (mDNS)

Instead of searching for Roku devices, your app will use a ZeroConf / mDNS network discovery plugin to scan the local Wi-Fi for _androidtvremote2._tcp.local. This returns the TV's local IP address.

